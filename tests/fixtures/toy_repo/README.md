# Toy repo

A tiny fixture repo with one intentional bug in `calculate_total()` (off-by-one:
skips the last item in the list). `test_calculator.py` catches it. Used to
exercise the Phase 1 agent loop without touching a real GitHub repo.
